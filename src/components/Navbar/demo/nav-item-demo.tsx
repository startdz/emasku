import React from 'react';

const NavBar = () => {
  return (
    <React.Fragment>
      <header>
        <div>
          <div>{/* Logo */}</div>

          <nav>{/* menu */}</nav>
        </div>
      </header>
    </React.Fragment>
  );
};

export default NavBar;
